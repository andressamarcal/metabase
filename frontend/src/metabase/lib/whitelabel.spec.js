
import { colorForScheme } from "./whitelabel"

import Color from "color";
import { brand } from "metabase/lib/colors";

describe("whitelabel", () => {
    describe("colorForScheme", () => {
        xit("should return the same color if the scheme is the brand color", () => {
        })
    })
})
